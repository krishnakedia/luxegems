Modify my ecommerce project so that the header and footer are hidden only on admin pages, but remain visible on all user-facing pages. Detect admin routes using /admin path (or role-based routing if available). Implement a proper solution using layout separation (AdminLayout and UserLayout) or route-based conditional rendering. Ensure the change does not affect the user side UI. Keep the solution scalable and production-ready.